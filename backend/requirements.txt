firebase-admin==6.5.0
# google-adk 2.6.3 requires pydantic>=2.12, which moved this off the old 2.8.2
# pin. All nine entity suites were re-run on 2.13.4 — see README.
pydantic==2.13.4
google-adk==2.6.3
# Pulled in by google-adk, but classify.py imports it directly (Vertex AI
# transport), so it is pinned here rather than relied on transitively.
google-genai==2.17.0
fastapi==0.141.1
# Serves the FastAPI app on Cloud Run.
uvicorn==0.52.1
# google-generativeai and python-dotenv were dropped when classify.py moved to
# Vertex AI: the `google-genai` client that google-adk already pulls in talks to
# Vertex on Application Default Credentials, so there is no API key to load.
# Only used by test_classify.py, to generate the unrecognizable test image.
pillow==12.3.0
# Required by fastapi.testclient, which test_api.py drives the API with.
httpx==0.28.1
